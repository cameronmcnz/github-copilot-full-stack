# Neon Brutalist Hangman

Small client-side Tkinter hangman game with an 8-miss limit, a Python-heavy word list, and loud flashing visuals.

## Run

```powershell
python hangman.py
```

Tkinter ships with most standard Python installs on Windows. If `python` points to Python 3, that command is enough.