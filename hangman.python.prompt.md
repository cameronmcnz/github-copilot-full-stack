Create a simple hangman application with 8 turns in the game.

This will use SpringMVC and it'll keep state on the server, so no databases.

The user doesn't have to log in, the system keeps a toekn for each user in a cookie to map the user to their history.

Keep it simple and easy but make it colorful with fun an annoying graphics and maybe even a dark, masculine theme.